# Dearlog 기능-취지 부합 점검 프롬프트

새 에이전트 세션에서 아래 내용을 그대로 붙여넣어 사용하세요. (서비스 헬스체크는
`SERVICE_CHECK_PROMPT.md` 참고 — 이 문서는 "기능이 서비스 취지대로 작동/구성되어
있는지"를 점검하는 별도 문서입니다.)

---

당신은 Dearlog의 핵심 기능들이 `PRD_Dearlog.md`에 정의된 서비스 취지대로 실제로
작동/구성되어 있는지 점검하는 작업을 맡았습니다. 작업 루트는
`/Users/moondh/Downloads/디어로그` 입니다. 한국어로 소통하세요.

먼저 `PRD_Dearlog.md`를 읽고 서비스 정의를 숙지하세요:

> 부모님(시니어)의 기억을 자녀(보호자)가 주도하는 AI 인터뷰로 수집하고, 가족이 함께
> 검수한 뒤 자서전·음성 아카이브·AI 분신 대화 형태로 오래 보존하는 가족 기억 아카이브 서비스.

역할명 주의: 코드 상 `role: 'parent'` = 답변하는 시니어(부모), `role: 'child'` = 질문을
준비하는 보호자(자녀). 실제 가족 호칭과 반대로 매핑되어 있음.

각 항목을 코드/DB/실제 동작(curl, sqlite 조회, 시뮬레이터 조작)으로 직접 확인하세요.
추측이나 "코드가 있으니 될 것이다"식 판단은 금지 — 실제로 실행해서 확인하세요. 문제를
발견하면 원인을 설명하고, 사소한 버그는 직접 수정하세요. 구조적/정책적 판단이 필요한
사안은 수정하지 말고 보고만 하세요.

## 1. 온보딩 및 초대 흐름 (보호자 → 시니어)

- 보호자 가입(`POST /api/auth/phone`)이 실제로 계정을 생성하는지.
- 초대 발급(`POST /api/invitations`)이 `dearlog://parent/autologin?token=...` 또는
  `https://dear-log.com/...` 형태 링크를 만들고, `INVITATION_TTL_DAYS`(기본 14일) 만료가
  실제로 DB에 반영되는지 (`Invitation.expiresAt` 확인).
- 시니어가 링크로 토큰 로그인(`/api/auth/token-login`) 시 1회용 처리가 실제로 동작하는지
  (`usedAt` 기록 후 같은 토큰 재사용 시 거부되는지 — 실제로 두 번 호출해서 확인).
- 보호자 마이페이지에서 초대 링크 재발급/폐기 기능이 실제 UI/API로 연결되어 있는지.

## 2. 인터뷰 흐름 (질문 설계 ↔ 음성 응답)

- PRD 6.1절 "기록 범위" 6개 카테고리(어린시절/가족/일과 생계/전환점과 감정/가치관과 남길
  말/사진 속 생활사)가 실제 공통 질문 풀(`Question` 시드/데이터)에 반영되어 있는지 확인.
- 사진 업로드 시 사진 기반 질문이 자동 생성되는 기능이 실제로 동작하는지 (관련 API/AI
  프록시 호출 경로 추적).
- 시니어의 음성 답변이 `InterviewRecord`/`FreeSpeechRecord`로 저장되고 STT 텍스트화가
  실제로 일어나는지 (원문 음성과 텍스트가 모두 보관되는지 — "근거 추적 가능"이 PRD 핵심
  주장이므로 실제 레코드를 조회해 음성 파일 경로 + STT 텍스트가 쌍으로 존재하는지 확인).
- 통화형 인터뷰 스케줄링(`InterviewSchedule`/`InterviewSession`)이 실제 예약 생성 →
  알림까지 연결되는지.

## 3. 검수 및 동의 (데이터 주권)

- 보호자가 답변을 검수/태그(`MemoryTag`) 처리하는 화면이 실제 데이터를 반영하는지.
- `MemoryConsentSettings`에서 기억별 "공개 범위 조정", "활용 중지", "완전 삭제"가 실제로
  DB 레벨에서 동작하는지 — 특히 "완전 삭제"가 실제로 레코드를 지우는지, 소프트 삭제로
  남아있어 PRD가 약속한 "데이터 주권"이 무색해지는 경우가 없는지 직접 삭제 테스트.

## 4. 자서전 제작 파이프라인

- `Chapter` 구성 → 문체 선택 → `CoverDesign` 확정 → `PublicationDraftCache` →
  `PublicationPreviewJob` → `PublicationRequest`로 이어지는 비동기 파이프라인이 실제로
  끝까지 도는지 (중간에 끊기는 단계가 있는지 로그/상태값으로 확인).
- A5 인쇄용 PDF가 jsPDF/React PDF로 실제 생성되는지, 생성된 PDF를 열어 레이아웃이
  깨지지 않았는지 확인.
- "실물 책 주문 준비 단계"가 실제 주문 플로우(결제 연동 여부 포함)로 연결되어 있는지,
  아니면 준비 단계에서 멈춰 있는 프로토타입인지 명확히 구분해서 보고.

## 5. 구독 재방문 루프 (월 구독 가치 제안 검증)

PRD 4절은 월 구독 가치를 "기억 검색, AI 분신 대화, 가족 질문, 주간 가족 퀴즈, 기념일
알림, 다음 인터뷰 예약"으로 정의합니다. 각각이 실제로 동작하는지 개별 확인:

- AI 분신 대화: `Memory`/`MemoryVectorEntry` 기반 검색 후 응답하는 구조가 실제로 임베딩을
  만들고 유사도 검색을 수행하는지 (하드코딩된 응답이 아닌지 확인).
- 가족 질문, 주간 가족 퀴즈: 실제 콘텐츠 생성/노출 로직이 있는지, 아니면 PRD에만 있고
  구현이 안 된 상태인지.
- 기념일/다음 인터뷰 알림(`Notification`/`PushSubscription`): 실제 푸시 발송 트리거가
  존재하는지, 푸시 구독이 등록되는지.
- 캘린더(`CalendarEvent`): 인터뷰 일정과 가족 이벤트가 실제로 캘린더에 반영되는지.

## 6. 디지털 유산 (실험적 기능 — 범위 확인만)

- `LegacyVault`가 PRD에 명시된 대로 "데모 전용" 상태를 유지하고 있는지 (실 사용자에게
  정식 기능처럼 노출되고 있지 않은지 UI 확인). 법무/감사 전 단계인데 일반 사용자 플로우에
  섞여 있다면 이슈로 보고.

## 7. 역할 분리 및 데이터 소유권

- `seniorId`/`guardianId` 기준 데이터 분리가 실제 API 라우트에서 강제되는지 —
  `docs/route-authorization-matrix.md`와 실제 라우트 코드를 대조해 문서와 구현이 일치하는지
  샘플링 점검 (전수 검사가 어려우면 최소 5개 라우트 무작위 샘플).
- 보호자 A가 다른 가족(보호자 B-시니어 B)의 데이터를 API로 직접 조회 시도했을 때 실제로
  차단되는지 (소유권 검사 실효성 검증 — 가능하면 실제 요청으로 테스트).

## 보고 형식

기능 단위로 아래 표를 작성하세요. "구성됨/일부 구성/미구현" 3단계로 평가하고, PRD가
약속한 가치와 실제 동작 사이에 괴리가 있으면 반드시 명시하세요.

| 기능 영역 | PRD상 약속 | 실제 상태 | 괴리/이슈 |
|---|---|---|---|
| 온보딩/초대 | | | |
| 인터뷰 | | | |
| 검수/동의 | | | |
| 자서전 제작 | | | |
| 구독 재방문 루프 | | | |
| 디지털 유산 | | | |
| 권한/데이터 분리 | | | |

마지막에 "사용자에게 보여줄 만큼 완성된 기능"과 "아직 프로토타입/약속만 있는 기능"을
구분해서 한 줄씩 정리하고, 발견한 사소한 버그는 수정한 목록을 별도로 보고하세요.
